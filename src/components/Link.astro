---
const { href, id = "", className = "" } = Astro.props;
---

<a
  id={id}
  href={href}
  class={`${className} w-full flex justify-center items-center gap-2 bg-white hover:bg-zinc-200 text-black text-base font-semibold px-6 py-3 rounded-xl cursor-pointer select-none`}
  data-astro-prefetch
>
  <slot />
</a>
